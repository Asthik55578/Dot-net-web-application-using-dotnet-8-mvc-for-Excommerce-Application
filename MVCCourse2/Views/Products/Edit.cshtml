@model MVCCourse2.Models.Product


@section title {
    @if(Model!=null)
    {
        <h3>Product:@Model.ProductName</h3>
    }
}

@if (Model != null)
{
    <partial name="_Product" model="Model" />
}